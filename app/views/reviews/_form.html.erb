_form.html.erb

<%= form_for(@restaurant, @review) do |f| %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.label :content %>
  <%= f.text_field :content %>

 <%= f.submit class: 'btn btn-primary' %>
<% end %>
